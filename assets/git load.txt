git config --global user.name "nombre"
git config --global user.email "email"
git config --global -e
:q
git config --global -l
:q

git init
git status
git add .
git status
git commit -m "header page php"
git status
git log
git Branch -M main o master
git remote add origin https://..............
git push origin master

//Recuperar borrado
git checkout -- .

//subir por separado
git add index.html
git status

//subir actualizaciones de todo
git add -u 

//no subir ciertor archivos previamente
git reset css/
git status
